server:
  port: 10008
logging:
  level:
    org.springframework.security: DEBUG
spring:
  application:
    name: cloud-suntak-activity
  cloud:
    config:
      uri: ${CONFIG_SERVER_ADDRESS}
      fail-fast: true
  profiles:
    active: ${ENVIRONMENT}
  sleuth:
    traceId128:
      true
    