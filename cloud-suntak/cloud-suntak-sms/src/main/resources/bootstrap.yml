server:
  port: 10006
logging:
  level:
    org.springframework.security: DEBUG
spring:
  application:
    name: cloud-suntak-sms
  cloud:
    config:
      uri: ${CONFIG_SERVER_ADDRESS}
      fail-fast: true
  profiles:
    active: ${ENVIRONMENT}
  sleuth:
    traceId128:
      true