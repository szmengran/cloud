server:
  port: 10005
logging:
  level:
    org.springframework.security: DEBUG
spring:
  application:
    name: cloud-suntak-ehr
  cloud:
    config:
      uri: ${CONFIG_SERVER_ADDRESS}
      fail-fast: true
  profiles:
    active: ${ENVIRONMENT}
    